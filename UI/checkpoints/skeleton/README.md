# Skeleton Checkpoint Placement

Place the skeleton model assets here:

- Checkpoint: `UI/checkpoints/skeleton/best.pt`
- Resolved config: `UI/configs/skeleton/config_resolved.yaml`

Do not commit real checkpoint files to git.
