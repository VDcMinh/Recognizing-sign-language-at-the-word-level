# Deprecated Region CNN-GRU Configs

These configs were moved out of `configs/train/` to keep the active workflow focused on `RegionResNet18GRU`.

Nothing here was deleted:

- old `RegionCNNGRU` configs remain available for reference and reproducibility
- region data and old outputs/checkpoints were not modified
- the `skeleton` branch was not changed
